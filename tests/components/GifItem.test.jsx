/* eslint-disable no-undef */
import { render } from '@testing-library/react'
import { GifItem } from '../../src/components'


describe('Tests on <GifItem />', () => {
  test('should', () => {

    render(<GifItem title='' url='' />)

  })
})
